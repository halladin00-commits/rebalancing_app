# 시안 대조 기준 (UI 리셋 작업용)

**기준은 `design_handoff_app_redesign/메인화면.dc.html` 실물이다.**
같은 폴더의 `README.md` 요약에는 폐기된 이전 버전(v9 계열) 내용이 섞여 있다.
실제로 요약에는 있으나 시안 v12a에는 **없는** 것: 포트 카드의 `조정 4` 배지, 비중 막대.
값이 엇갈리면 항상 HTML을 연다.

읽는 법: `sed -n '2440,2572p' "design_handoff_app_redesign/메인화면.dc.html"`

---

## 화면 위치 (HTML 줄 번호)

확정본만 적는다. v2~v11, v19, v21은 폐기.

| 화면 | id | 줄 | 상태 |
| --- | --- | --- | --- |
| 자산 탭 | v12a | 2440 | ✅ 대조 완료 |
| 포트 상세 | v12b | 2573 | ✅ 스파크라인·more_vert 남음 |
| 종목 상세 (거래내역) | v12c | 2730 | ✅ 스파크라인 남음 |
| 종목 상세 (간편입력) | v12d | 2857 | ❌ |
| 첫 진입 | v12e | 2975 | — v17d로 대체 |
| 종목 검색 | v13a | 3065 | ✅ |
| 거래 추가 | v13b | 3141 | ❌ |
| 포트 추가 | v13c | 3228 | ✅ |
| 포트 메뉴 | v13d | 3290 | ✅ |
| 갱신 3상태 | v13e | 3367 | ✅ 실패 표시 |
| 해외 거래 추가 | v14a | 3425 | ❌ |
| 거래 추가 기본 | v15a | 3572 | ✅ |
| 거래 설정 시트 | v15b | 3653 | ❌ |
| 거래 내역 전체 | v16a | 3757 | ✅ |
| 삭제 확인 | v16b | 3878 | ✅ |
| 순서 변경 | v16c | 3960 | 🔶 편집 모드로 존재 |
| 예수금 | v16d | 4027 | ❌ |
| 검색 결과 없음 | v16e | 4152 | ✅ (검색 화면에 포함) |
| 수수료 설정 | v17a | 4248 | ❌ |
| 업로드 파일 선택 | v17b | 4352 | ✅ |
| 업로드 검증 | v17c | 4424 | ✅ |
| 첫 진입 개정 | v17d | 4527 | ✅ |
| 리밸런싱 탭 | v18a | 4628 | 🔶 |
| 포트 진단 | v18b | 4743 | 🔶 |
| 목표 설정 | v18c | 4872 | ✅ |
| **조정 제안** | v18d | 4996 | ✅ |
| 조정 제안 개정 | v20a/b/c | 5600/5726/5859 | ✅ |
| 전체 결산 | v22a | 6281 | 🔶 제외 띠 완료 |
| 포트 결산 | v22b | 6451 | 🔶 제외 띠 완료 |
| 기간 점프 시트 | v22c | 6614 | ✅ |
| 더보기 | v23a | 6712 | 🔶 소수점 행 완료 |
| 소수점 거래 설정 | v23b | 6827 | ✅ |

---

## 공통 골격 (v12a · v12b에서 확인)

### 딥그린 헤더
- 배경 `#0E4F49`, 하단 라운드 28, 아래 패딩 16
- 상태바 44 + 타이틀 행 **48** (현재 `BrandHeader`는 46 — 차이)
- 탭 루트: 로고 21px + `REBALANCING` 11/700 자간 3 `rgba(255,255,255,.78)`
  (현재 `AppLogo`는 26px 불투명 흰색 — 차이)
- 상세: `arrow_back 23 #fff` + 이름 15/700 자간 −.2 + `refresh 21` + `more_vert 21`
  (아이콘 `rgba(255,255,255,.86)`)
- 내용 패딩 `4px 22px 0`

### 헤더 본문 (자산 탭·포트 상세 동일)
```
라벨 12.5/600 rgba(255,255,255,.76)   [우] 환율 칩 10.5/700 흰색 on rgba(255,255,255,.16) 라운드 20, 패딩 4×9
금액 35/800 #fff 자간 −1.5 line-height 1.08, margin-top 4      ← 민트가 아니라 흰색
타일 2개 (gap 9, margin-top 13): flex 1, bg rgba(0,0,0,.10), 라운드 13, 패딩 10×12
   라벨 11.5/600 rgba(255,255,255,.78)
   금액 16/700 #8FE7B0 자간 −.3 (mt 4)
   %    11.5/600 #8FE7B0 (mt 2)
스파크라인 높이 46, margin-top 14, 선 #8FE7B0 2.2px, 아래 그라데이션 .32→0
하단 줄 10.5/500 rgba(255,255,255,.76) (mt 4): [최근 12개월] ⟷ [08.17 09:41 기준]
```

### 본문
- 패딩 `14px 16px 0`, 카드 사이 gap 9~10
- **목록 카드**: 흰 배경, 라운드 20, 좌우 패딩 18, 그림자 `0 1px 2px rgba(22,19,15,.05)`
  행 사이 1px `#EFEADC` 구분선 (마지막 행 없음) → `widgets/list_card.dart`
- **행 우측**: 금액 15/700 자간 −.3 / 아래 gap 8 baseline
  전일 `▲0.52%` 11~11.5/600 `#726B5F` + 누적 `+8.03%` 12.5/700 손익색
- `chevron_right` 20 `#8A8478`, margin-right −4
- **추가 카드**: 높이 46, 라운드 16, 점선 1.5 `#D6CFBC`, `add` 19 + 13/700 `#0E4F49`
- 배너 50 + 탭바 64 (하단 패딩 6)

### 행 이름 크기
- 포트폴리오 행 **15.5**/700 자간 −.2
- 종목 행 **14.5**/700 자간 −.2, 보조 줄 `₩21,450 · 220주` 11.5/600 `#6B6357`
  (곱셈 기호 ×가 아니라 **점 구분자**, **가격이 먼저**)
- 간편입력 종목은 보조 줄 옆에 `history_toggle_off` 14 `#A39B8C`

### 시장 칩
10.5/800, 라운드 5, 패딩 3×5 — KR `#1E4E6B`/`#DCE9F0` · US `#4C2A72`/`#EBE4F3` · 현금 `#3F5218`/`#E9EFDC`

### 섹션 제목
좌 13/800 자간 −.2 + 우 부가 11.5/600 `#6B6357` (예: `3종목 · 예수금 포함`)

---

## v12a 자산 탭 — 액션 카드 2개

flex 1, 라운드 **16**, 패딩 `11px 12px 12px`, gap 9

| | 조정 필요 | 결산 준비 |
| --- | --- | --- |
| 배경 | `#F5EEDF` | `#E6EEEC` |
| 아이콘 | `error` 16 `#9C4A16` | `insights` 16 `#0E4F49` |
| 라벨 | 11/700 `#9C4A16` 자간 .2 | 11/700 `#0E4F49` |
| 이름 | 13/700 `#4A3512` (mt 8) | 13/700 `#123D39` |
| 부가 | 11.5/500 `#7A5F22` (mt 3) `4종목 · 최대 +5.08%p` | 11.5/500 `#3D5C58` `전월 +2.31%` |

우측 상단 `chevron_right` 16, 같은 색.
**미구현**: `insights` 아이콘, 세 전용 글자색, 결산 부가를 `전월 +2.31%`로.

---

## v12b 포트 상세

- **탭 없음.** 자산/리밸런싱 탭은 시안에 존재하지 않는다. 리밸런싱은 하단 탭에서만.
- 헤더는 위 공통 골격 + 라벨 `평가금액`
- 본문: `구성 종목` 섹션 제목 + `3종목 · 예수금 포함` → 목록 카드(행 패딩 12) → 종목 추가
- 액션은 **`more_vert` 메뉴**. SpeedDial FAB는 시안에 없다.

---

## v18d 조정 제안 (다음 작업)

헤더: 배경 `#0E4F49` (라운드 없음), 타이틀 행 52
`arrow_back 22 #fff` + [제목 15/700 `조정 제안` / 부제 11/600 `rgba(255,255,255,.7)` `연금저축 ETF · 매도 1건`]

본문 패딩 `13px 16px 0`, gap 9

**1. 모드 세그먼트** — 지금의 투자금 입력 필드를 대체한다
```
bg #F2EDE1, 라운드 13, 패딩 4, gap 6
칸: flex 1, 높이 36, 라운드 10
활성 bg #0E4F49 + 12/800 #fff  ·  비활성 12/700 #6B6357
[보유 안에서] [추가 입금으로]
```

**2. 조정 카드** — 흰 배경, 라운드 18, 그림자 동일, overflow hidden
```
[종목 블록] 패딩 13px 16px 14px, 아래 1px #EFEADC
  행1: 매도 배지(폭 34, 11/800 #B85127 on #FBEDE6, 라운드 6, 패딩 4×0, 가운데)
       + 이름 14/700 자간 −.2 + check_circle 20 #0E4F49
  행2 (mt 11, align flex-end):
       좌: 수량 24/800 자간 −.6 line-height 1.1  ·  아래 11.5/600 #6B6357 `현재가 ₩21,450 기준`
       우: 금액 15/700 자간 −.3  ·  아래 11.5/600 #6B6357 `40.08% → 34.98%`
  행3 (mt 11): bg #F7F3EA 라운드 11 패딩 9×11
       savings 16 #0E4F49 + 11.5/600 #4A443B `매도 대금이 예수금으로 들어가 10.19% → 15.29%`

[편차 블록] 패딩 12px 16px 14px
  좌 12/700 #4A443B `조정 후 남는 편차` · 우 11.5/600 #8A8478 `허용 ±3%p`
  (mt 7) 15/700 #B85127 `+5.08%p` → arrow_forward 17 #A39B8C → 18/800 #0E7A52 `−0.30%p`
         우측 배지 10.5/800 #0E7A52 on #E3F3EA 라운드 20 패딩 4×8 `허용 안`
  (mt 7) 11/600 #8A8478 line-height 1.5 — 남는 편차의 이유 한 줄
```

**3. 반올림 설명 카드** — 흰 배경, 라운드 18, 패딩 `12px 16px 13px`
```
rule 18 #726B5F + 12.5/700 `단주는 반올림` + 우 11.5/600 #6B6357 `27.9주 → 28주`
(mt 7) 11/500 #726B5F line-height 1.55 — 버림 대비 반올림이 목표에 가깝다는 설명
```

**4. 안내 줄** — 패딩 `0 4px`, `info` 15 `#8A8478` + 11/500 `#726B5F`
`이 제안은 주문을 내지 않습니다 — 실제 매매 후 아래 버튼으로 기록하세요.`

**5. 하단 CTA** — 패딩 `10px 16px 16px`, gap 8
- 주 버튼: flex 1, 높이 50, 라운드 14, `#0E4F49`, 13.5/700 흰색 `거래 내역으로 기록`
- 보조: 50×50, 라운드 14, 흰 배경 + 1.5px `#D6CFBC`, `share` 21 `#0E4F49`

**선행 작업**: `_RebalanceTransactionDialog`가 `portfolio_detail_screen.dart`에 private으로
묶여 있다. 별도 파일로 빼야 이 화면에서 쓸 수 있다.

---

## 남은 큰 덩어리 (다음 작업 순서 제안)

1. **v12b·v12c 헤더 스파크라인** (판단 필요) — 포트별·종목별 12개월 추이.
   지금 `AssetHistoryService`는 전체 자산만 기록한다. 포트/종목 단위 기록이
   필요하거나, 종목은 `ApiService.fetchWeekPrices`로 주가 추이를 받아야 한다.
2. **v15b 수수료·환율·예수금 설정 시트** — 거래 추가 화면에서 부르는 시트.
   `item_form_dialog`(547줄)는 이제 '직접 등록·편집' 전용으로만 남았다.
4. **v16a~e** 거래 내역 전체·삭제·순서·예수금·검색 없음
5. **v17a~d** 수수료 설정·업로드·첫 진입
6. **v22b 손익 구성 표** — ⚠️ **확인 필요**. 시안은 4행(매도 실현손익 /
   배당 / 평가손익 변동 / 수수료·세금)인데 **배당과 실제 수수료는 앱에 데이터가
   없다** (`StockTransaction`은 수량·단가만 가진다). 만들려면 배당을 거래 유형으로
   추가해야 한다. 지금 만들면 두 행은 숫자를 지어내는 셈이다.
7. **v12a 결산 준비 카드** — 완료 (전월 수익률 지연 로딩)

## 모델 확장이 필요한 것 (사용자 확인 필요)

아래 시안들은 **앱에 없는 데이터**를 요구한다. 지금 만들면 숫자를 지어내야 한다.
셋 다 뿌리가 같다 — `StockTransaction`이 수량·단가만 가진다.

| 시안 | 필요한 것 |
| --- | --- |
| v22b 손익 구성 표 | **배당**을 거래 유형으로. 실제 **수수료·세금** 기록 |
| v16d 예수금 | 예수금 **입출금 이력**. 지금 현금은 잔액 숫자 하나뿐 |
| v17a 수수료·환율 시트 | 거래별 **수수료율·적용 환율·예수금 차감** 필드 |

한 덩어리로 보면: `StockTransaction`에 `type`(매수/매도/배당/입금/출금),
`fee`, `tax`, `fxRate`를 더하고 입력 화면과 결산 계산을 따라 고치는 일이다.
기존 저장 데이터 호환(기본값)이 필수다.

## 검증 환경 주의

에뮬레이터(`Medium_Phone_API_36.1`)가 **Gradle 빌드와 동시에 돌면 죽는다.**
빌드가 CPU·메모리를 다 먹어 에뮬레이터가 굶는다. 죽은 뒤에는 패키지 매니저가
깨져 `am start`가 "Activity class does not exist"를 내며, 재부팅으로는 안 고쳐진다.

**순서를 지킨다: 빌드를 먼저 끝내고 → 그 다음 에뮬레이터를 띄운다.**

### 에뮬레이터의 ANR은 대부분 AdMob이다

`Rebalancing isn't responding`이 반복되면 로그를 먼저 본다:

```
adb logcat -d | grep -i "gms.ads\|DynamiteModule"
```

`com.google.android.gms.ads.dynamite` 원격 모듈 로딩이 몇 초씩 걸리는 게
보이면 **앱 문제가 아니다.** Play 서비스가 차가운 에뮬레이터에서 광고 SDK가
메인 스레드를 잡는 것이고, 실기기에서는 훨씬 가볍다. 실제로 사용자 폰
(SM-S921N)에서는 같은 빌드가 문제없이 돌았다. 쫓지 말 것.

**검증할 때는 비행기 모드를 켠다.** 광고가 즉시 실패해 ANR이 아예 안 난다.
시세는 이미 받아 둔 값이 남아 있어 화면 확인에는 지장이 없다.

```
adb shell cmd connectivity airplane-mode enable    # 검증 시작 전
adb shell cmd connectivity airplane-mode disable   # 시세 갱신을 봐야 할 때만
```

깨졌을 때 복구:
```
taskkill //F //IM qemu-system-x86_64.exe
adb kill-server && adb start-server
"$LOCALAPPDATA/Android/Sdk/emulator/emulator.exe" -avd Medium_Phone_API_36.1 -wipe-data -no-snapshot -no-boot-anim &
```

## 진행 규칙

1. 화면 하나를 **끝까지** 만들고 실기기로 확인한 뒤 다음으로 넘어간다.
   색만 바꾸고 다음 단계로 넘어가지 않는다.
2. 기능 접근 경로를 끊지 않는다. 상세의 리밸런싱 탭은 v18d가 완성된 **뒤에** 없앤다.
3. 색은 `main.dart`의 `AppColors` 토큰만 쓴다. 손익 색은 반드시 `PnlColorNotifier`.
4. 치수는 `theme/design_system.dart`의 `DS`. 시안에만 있는 값은 DS에 추가한다.
5. 자식 없는 박스로 색·선·막대를 그리지 않는다 (크기 0으로 사라진다).
   → `[[project-flutter-zero-size-pitfall]]`
