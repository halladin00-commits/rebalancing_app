name: rebalancing_app
description: 포트폴리오 리밸런싱 계산 앱
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  http: ^1.2.0
  path_provider: ^2.1.2
  json_annotation: ^4.8.1
  provider: ^6.1.1
  cupertino_icons: ^1.0.6
  screenshot: ^3.0.0
  image_gallery_saver_plus: ^4.0.1
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/kr_stocks.json
